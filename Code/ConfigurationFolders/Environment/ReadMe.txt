The file names are objects names

HasReturn means the method has a return value
NoReturn means the method does not have a return value

BasicValue means the return value is a number, string, date and the types that can become inputs
Object means the return value is an object

 